{
  "name": "vyarna-nucleus",
  "version": "1.0.0",
  "workspaces": [
    "packages/*",                             # shared libs
    "apps/*",                                 # mobile, web, admin, marketing sites
    "services/*/*"                            # all domain services (incl. vy-gateway)
  ],  
  "scripts": {
    "dev": "turbo run dev",
    "prepare": "husky install .husky",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "latest"
  },
  "author": "Pedro Alvarado"
}
